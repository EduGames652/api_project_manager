const path = require('path')

module.exports = {
  config: path.resolve(__dirname, 'api', 'config', 'database', 'database.js'),
  'migrations-path': path.resolve(__dirname, 'api', 'database', 'migrations'),
}
